# ============================================================
# REQUIRED — the app won't start without these.
# ============================================================

# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service-role key. Bypasses RLS; used only by server-side
# routes (the webhook and automation engine). Keep this secret —
# never paste it into client code.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# WhatsApp token encryption (64 hex chars = 32 bytes, AES-256-GCM).
# Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Rotating this value orphans every token encrypted under the previous
# key — users have to re-save their WhatsApp settings to reconnect.
ENCRYPTION_KEY=your-64-char-hex-key-here

# Meta App Secret (Meta for Developers → App Settings → Basic).
# Verifies the HMAC-SHA256 signature on every inbound webhook POST, and
# (if WhatsApp Coexistence is configured below) exchanges the Embedded
# Signup authorization code for an access token server-side.
# Required — without it the webhook rejects every request.
META_APP_SECRET=your-meta-app-secret

# ============================================================
# RECOMMENDED — safe defaults exist but you'll want to set these.
# ============================================================

# Canonical public URL of this deployment (scheme + host, no trailing
# slash). Used for the sitemap, OG images, and any email the app
# sends. Defaults to https://MJChatSyncs.tech if unset.
NEXT_PUBLIC_SITE_URL=https://crm.example.com

# ============================================================
# WHATSAPP COEXISTENCE (Embedded Signup) — optional.
# ============================================================
#
# Lets any workspace on this CRM connect a WhatsApp number that's
# already active on the WhatsApp Business app, via Meta's Embedded
# Signup "Connect existing WhatsApp Business app" flow — the number
# then stays usable on the app AND this CRM's Cloud API at once.
#
# These belong to THIS PLATFORM's own Meta app (you, as Tech
# Provider/Solution Partner) — set once here, not per customer. Every
# workspace's own customers authenticate with their own Meta Business
# account through the same app. Requires a Facebook Login for Business
# configuration built from the WhatsApp Embedded Signup template with
# the "WhatsApp Business app onboarding" (Coexistence) option enabled,
# and this deployment's domain listed in that app's Allowed Domains /
# Valid OAuth Redirect URIs.
#
# The "Conectar via Coexistência" button in Settings > WhatsApp stays
# hidden automatically while these are unset.
NEXT_PUBLIC_META_APP_ID=your-meta-app-id
NEXT_PUBLIC_META_CONFIG_ID=your-embedded-signup-configuration-id

# ============================================================
# OPTIONAL — only needed if you use the feature.
# ============================================================

# Shared secret protecting GET /api/automations/cron. Required if you
# use Wait steps in automations (a scheduled pinger drains pending
# executions). Generate any long random string:
#   openssl rand -hex 32
# See docs/automations-and-cron.md.
# AUTOMATION_CRON_SECRET=generate-a-long-random-string

# ============================================================
# AI ENGINE (optional — AI features are disabled without these)
# ============================================================

# Provider: nvidia (default) | gemini | openai | claude | openrouter | local
AI_PROVIDER=nvidia

# NVIDIA NIM API (Required if AI_PROVIDER=nvidia)
NVIDIA_API_KEY=nvapi-your-key-here
NVIDIA_MODEL=nvidia/llama-3.1-nemotron-70b-instruct
NVIDIA_EMBEDDING_MODEL=nvidia/nv-embed-v2
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1

# Google Gemini (if AI_PROVIDER=gemini)
# GEMINI_API_KEY=

# OpenAI (if AI_PROVIDER=openai)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o

# Anthropic Claude (if AI_PROVIDER=claude)
# ANTHROPIC_API_KEY=

# OpenRouter (if AI_PROVIDER=openrouter)
# OPENROUTER_API_KEY=

# Local LLM — any OpenAI-compatible endpoint (if AI_PROVIDER=local)
# LOCAL_LLM_BASE_URL=http://localhost:11434/v1
# LOCAL_LLM_MODEL=llama3

# AI Behavior Settings
AI_CONFIDENCE_THRESHOLD=0.7
AI_MAX_HISTORY_MESSAGES=20
AI_MAX_KNOWLEDGE_CHUNKS=5
AI_SUMMARY_TRIGGER_COUNT=50

# MongoDB Atlas AI Database Configuration
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net
MONGODB_DB_NAME=MJChatSyncs
